name: Release checks
on:
  push:
    branches: [main]
    paths-ignore:
      - .release-check-result
  workflow_dispatch:
permissions:
  contents: write
concurrency:
  group: release-checks-${{ github.ref }}
  cancel-in-progress: true
jobs:
  verify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
      - id: install
        run: npm ci
      - id: syntax
        run: npm run check
      - id: smoke
        continue-on-error: true
        run: node test/release-smoke.mjs > smoke.log 2>&1
      - id: live
        continue-on-error: true
        run: |
          for i in {1..12}; do
            body="$(curl -fsS https://gmach-karov1.nh8030954.workers.dev/api/health || true)"
            echo "$body" > live.log
            if echo "$body" | grep -q '"release":"complete-platform-2026-09-25.9"' && echo "$body" | grep -q '"completePlatformSchema":{"ready":true' && echo "$body" | grep -q '"adminCredentialRotated":true'; then exit 0; fi
            sleep 10
          done
          exit 1
      - name: Inspect live platform configuration
        if: always()
        run: |
          curl -fsS https://gmach-karov1.nh8030954.workers.dev/api/platform/features > platform-features.log || echo '{}' > platform-features.log

      - name: Record verification result
        if: always()
        continue-on-error: true
        run: |
          printf 'sha=%s\ninstall=%s\nsyntax=%s\nsmoke=%s\nlive=%s\n' "$GITHUB_SHA" "${{ steps.install.outcome }}" "${{ steps.syntax.outcome }}" "${{ steps.smoke.outcome }}" "${{ steps.live.outcome }}" > .release-check-result
          if [ -f smoke.log ]; then tail -n 12 smoke.log >> .release-check-result; fi
          if [ -f live.log ]; then tail -n 3 live.log >> .release-check-result; fi
          if [ -f platform-features.log ]; then echo "platform_features=$(cat platform-features.log)" >> .release-check-result; fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .release-check-result
          git commit -m "Record release check result" || true
          git push
      - name: Enforce smoke success
        if: always()
        run: test "${{ steps.smoke.outcome }}" = "success" && test "${{ steps.live.outcome }}" = "success"
